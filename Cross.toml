# 仅在使用 LOGKIT_PACK_LINKER=cross 时需要 Docker + cross；默认 logkit-pack.sh 在 macOS 用 Zig，可不使用本文件。
# 文档: https://github.com/cross-rs/cross

[target.x86_64-unknown-linux-musl]
[target.aarch64-unknown-linux-musl]
