---
description: Git 提交说明：单行 subject；仅重大改动才写 body（第二段）。
globs: "**/*"
alwaysApply: false
---

# 提交信息（logkit）

## Subject（必填，一行）

- 使用 **`feature:`**、**`fix:`**、**`refactor:`**、**`docs:`**、**`chore:`** 等前缀（与仓库习惯一致即可），后跟**简短中文动宾短语**。
- **只写一行**，概括本次改动的主要结果；不要写成文件清单或路径枚举。
- 优先写可感知结果（行为、协议、配置、排障），例如：
  - `feature: Kafka sink 支持 record headers 配置`
  - `fix: 修正实例 YAML 合并后 sink 类型校验错误`
- 避免模糊词：「调整一下」「改点东西」「小修小补」等。

## Body（第二段，**仅重大时写**）

在 subject **空一行**之后写 body。**只有**下列情况需要写 body（1～2 段即可）；**其余**日常提交 **不写 body**，subject 说清即可。

- **重大功能**：新能力跨多模块、对外协议/配置形态变化、用户需迁移或显式知晓的行为变更。
- **重大重构**：目录/ crate 边界调整、公共 API 语义变化、大范围删改且影响后续开发路径。
- **重大 fix**：数据正确性、安全、生产事故级缺陷，或修复牵涉面广需在提交里说明根因与影响面。

Body 里建议包含（不必条条写满，择要即可）：

- **为什么**做这次改动；
- **设计或边界**上需要同事/未来的自己一眼能懂的重点；
- **兼容性 / 迁移**注意点（若有）。

避免：逐文件流水账、与 subject 完全重复的废话。

## 避免

- 不要在提交信息末尾追加「来自 Cursor」「Generated by Cursor」等无关署名。
- 不要 subject 英文、body 中文的随意混搭，除非仓库已有约定。
- 非「重大」类别不要为了凑格式强行写空 body或套话第二段。

## 提交前（Rust）

- 有 **`*.rs` / `Cargo.toml`** 变更时，先按 **`.cursor/rules/rust-pre-commit-clippy.mdc`** 在仓库根执行 **`cargo clippy --workspace --all-targets -- -D warnings`** 并修复至通过，再提交。
