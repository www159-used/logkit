---
description: Rust 单测命名与文档注释（英文用例名 + 中文三段说明），与 JUnit/ScalaTest 规则对齐。
globs:
  - "**/src/**/*.rs"
  - "**/tests/**/*.rs"
alwaysApply: false
---

# Rust：`#[test]` 命名与文档注释

适用范围：本仓库内 **`#[cfg(test)] mod …`** 内联单测、以及 **`tests/`** 目录下的集成测试；**`#[test]`** / **`#[tokio::test]`** 等异步测试同理。

## 用例命名

- 测试函数名使用 **英文**（`snake_case`），简短可读，概括场景或断言焦点。
- 不必把整段 YAML / 配置全文塞进函数名；细节写在下方三段注释里。

## 文档注释（每条用例紧挨 `#[test]` 上方）

- 使用 Rust 文档注释 **`///`**（多行，每行一条 `///`），紧挨在 **`#[test]`**（或 `#[tokio::test]`）**之上**。
- 正文使用 **中文**。
- **固定三段**，按顺序书写（段标题与 kafka 参考规则一致，便于检索）：

  1. **`测试内容：`** — 覆盖的场景、规则或边界。
  2. **`输入：`** — 关键输入、前置条件或数据要点（不必重复整段时可写要点）。
  3. **`预期：`** — 期望结果、关键断言或失败形态。

每段一句或多句均可，**简短优先**。

## 示例

```rust
    /// 测试内容：Kafka sink 的 `headers` 能从 YAML 反序列化且与传输相关可选字段分离。
    /// 输入：含 `source`、`trace-id`、null 与整数字段的 `sink.kafka.headers`。
    /// 预期：`headers` 各键类型正确；未设置 `acks`、`compression` 等可选字段。
    #[test]
    fn deserialize_producer_yaml_kafka_headers() {
        // ...
    }
```

## 与生产代码同文件时

- 单测写在 `src/**/*.rs` 的 `#[cfg(test)] mod tests { … }` 中时，同样遵守上述 **`///` 三段**；新增用例必须带齐三段，避免只写断言不写意图。
