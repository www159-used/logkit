# logen-config

`logen-config` 提供 `logen` / `logend` 共用的 TOML 解析与合并逻辑。

详细说明统一放在 book：

- [client / logend 配置参考](../guide/src/cli/reference/config.md)
- [logend 守护进程](../guide/src/cli/reference/daemon.md)

参考默认配置见 [`assets/conf.ref.toml`](assets/conf.ref.toml)。
