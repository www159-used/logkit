# musl 默认可执行文件为 static-pie，无法 dlopen；pullout native 路径需 -crt-static + libdl。
[target.aarch64-unknown-linux-musl]
rustflags = ["-C", "target-feature=-crt-static", "-C", "link-arg=-ldl"]

[target.x86_64-unknown-linux-musl]
rustflags = ["-C", "target-feature=-crt-static", "-C", "link-arg=-ldl"]
