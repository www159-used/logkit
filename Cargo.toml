# logkit — 根工作区：logspout（CLI）、logspout-daemon、logspout-dsl、logspout-config、logspout-proto。
[workspace]
resolver = "2"
members = [
    "logspout-config",
    "logspout-proto",
    "logspout",
    "logspout-daemon",
    "logspout-dsl",
]

[workspace.dependencies]
clap = { version = "4", features = ["derive", "env"] }
